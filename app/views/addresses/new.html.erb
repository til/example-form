<% content_for :title, "New address" %>

<small>Full page loaded at <%= Time.now.strftime("%H:%M:%S") %></small>
<h1>New address</h1>

<%= render "form", address: @address %>

<br>

<div>
  <%= link_to "Back to addresses", addresses_path %>
</div>
